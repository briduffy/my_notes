<h1 class="center-align">Post a Note</h1>

<p><%= link_to 'Jot Something Down', new_note_path %></p>

<% @notes.each do |note| %>
  <h3><%= note.subject %></h3>
  <%= link_to 'View Note', note_path(note) %>
<% end %>